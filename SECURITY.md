# 보안

이 앱은 브라우저 로그인 세션과 저장된 자격증명을 다룬다. 이 문서는 무엇을 어떻게 다루는지와
무엇을 막지 못하는지를 적는다.

## 다루는 자격증명과 처리 방식

쿠키 임포트. 사람이 고른 Chrome/Brave/Edge 프로필의 쿠키를 복호화해 앱의 브라우저
파티션에 넣는다. 그 프로필로 로그인해 둔 사이트에 앱 안에서도 로그인된 채로 들어가기 위한 것이다.
복호화에는 그 기기의 Keychain 접근이 필요하므로 macOS가 사람에게 한 번 묻는다.

비밀번호 임포트(선택, 기본 꺼짐). 계정 화면에서 명시적으로 켠 경우에만 Chrome의 로그인
DB를 복호화해 아이디·비밀번호를 가져온다. 가져온 값은 `safeStorage`(Keychain 기반)로 암호화해
`~/.iris/creds.enc`에 두고, 파일 권한은 0600이다. 암호화가 불가능한 환경에서는
디스크에 쓰지 않는다.

자동완성. 로그인 칸을 누르면 그 origin 에 저장된 아이디만 목록으로 표시된다. 비밀번호는
사람이 계정을 고른 그 순간 하나만 꺼내 전달하며, 메인 프로세스가 파티션·origin·아이디를
다시 확인한다. 목록 응답에는 비밀번호가 실리지 않는다.

AI 와 자격증명. AI 가 조작 중인 탭에는 비밀번호를 전달하지 않는다. AI가 자동완성으로 로그인할
수 있는 (사이트, 아이디) 조합은 사람이 허용 목록에 올린 것뿐이고, 그 목록에도 비밀번호 값은 없다.
예외는 로컬 개발 서버 하나다. 주소가 로컬이고 그 포트를 듣는 프로세스의 작업 폴더에 프로젝트가
심어 둔 `.iris-local.json`이 있으면, 허용 목록 없이도 그 파일의 개발 계정으로 채운다. 원격 주소에는
이 예외가 적용되지 않는다. 채운 비밀번호는 명령 결과·로그·녹화에서 가려진다.

사용량(선택 기능). 켜져 있으면 서버가 시작할 때와 그 뒤 5분마다, 이 컴퓨터에 깔린 에이전트
CLI 들이 저장해 둔 로그인 토큰을 읽어 각 제공자의 사용량 조회 주소에 읽기 요청을 보낸다.
읽는 위치는 Claude Code(macOS Keychain의 `Claude Code-credentials` 항목과 `~/.claude` 아래
자격증명 파일), Codex(`~/.codex` 아래 `auth.json`), Gemini CLI(`~/.gemini`), OpenCode, Kimi Code,
Grok 의 홈이고, 홈을 옮긴 경우 `CLAUDE_CONFIG_DIR`·`CODEX_HOME`·`KIMI_CODE_HOME`·`GROK_HOME`을
따른다. Claude Code 는 Keychain 항목과 파일을 둘 다 읽어 만료가 더 뒤인 토큰을 쓰므로, 홈을
옮겼어도 Keychain 에 기본 계정이 남아 있으면 그 계정의 사용량이 보일 수 있다. 토큰을 갱신하거나
다른 곳에 보내지 않으며, 외부 상태를 바꾸는 요청은 사람이 화면에서 누른 Codex 초기화권
하나뿐이다. OpenCode·MiniMax 는 설정 화면에 사람이 직접 넣은 쿠키로
조회한다. 이 기능을 원하지 않으면 설정 → 편의 기능에서 「사용량」을 끄고 앱을 다시 실행한다.
그 뒤로는 서버가 어느 토큰도 읽지 않는다.

## 알려진 한계

- 앱 프로세스가 침해되면 자격증명도 침해된다. `safeStorage` 는 디스크에 있는 값을 지키지,
  실행 중인 앱의 메모리를 지키지 않는다.
- 쿠키 스테이징은 평문이다. Electron API 가 거절한 쿠키를 원본 그대로 넣기 위해 임시 SQLite
  파일을 거치는데, `sqlite3`가 읽어야 해서 암호화할 수 없다. 그래서 오래 두지 않는다.
  하루가 지나거나 세 번 실패하면 삭제한다. 위치는 상태 폴더가 아니라 Electron 앱 데이터 폴더
  (`~/Library/Application Support/Iris/cookie-import-staging`, 개발 환경은 `Iris-dev`) 아래이고 권한은
  0700/0600이다.
- 사용량 쿠키는 평문이다. 설정에 넣은 OpenCode·MiniMax 쿠키는 상태 폴더의 `usage.json`에
  그대로 들어가고, 서버가 그 파일을 열고 쓸 때 권한을 0600으로 맞춘다. 서버 프로세스에는
  Keychain 기반 암호화가 없어서 다른 CLI 의 인증 파일과 같은 수준으로 둔다. 창에는 값의 존재 여부만
  전달되고 값 자체는 전달되지 않는다.
- 프로필 격리는 조회 단계까지다. 한 프로필에서 가져온 계정은 기본적으로 그 프로필 탭에서만
  표시된다. `IRIS_CRED_SHARE_PARTITIONS=1`로 켜면 프로필 경계를 넘어 합쳐 보여주며, 그때는 격리가 없다.
- 원격 접속은 네트워크 경계에 의존한다. `REMOTE=1` 은 0.0.0.0 에 바인딩하고, 접속하는 쪽의
  IP 주소가 루프백이거나 100.64.0.0/10(Tailscale이 tailnet 기기에 주는 CGNAT 대역)일 때만 받는다.
  확인하는 것은 그 주소 값뿐이다. Tailscale 이 실제로 준 주소인지는 확인하지 않으므로, 같은 대역을
  쓰는 다른 망(통신사 CGNAT 등)에 물린 채 켜면 그 망의 기기도 들어올 수 있다. 그 안에서는 별도
  인증이 없다. `HOST`를 따로 주면 바인딩 주소는 그 값이 우선하고(`REMOTE` 와 무관), 허용 대역
  검사는 그대로다.
- 프록시를 거친 요청은 받지 않는다. `X-Forwarded-For`·`Forwarded`·`X-Real-IP` 헤더가 붙은 HTTP 요청과
  WebSocket 연결은 거부한다. 같은 기기의 리버스 프록시가 외부 요청을 루프백으로 넘기면 서버가 보는
  주소가 127.0.0.1 이 되기 때문이다. 이 헤더를 붙이지 않는 프록시를 앞에 두면 그 요청은 로컬 요청과
  구별되지 않는다. 터미널, 실행, git 조작, `/browser-cmd`, 기능 설정 변경은 접속 주소가 루프백이고
  위 헤더가 없을 때만 허용한다.
- 브라우저 요청은 이 서버 자신의 origin 에서 온 것만 받는다. `Origin` 이 `http://127.0.0.1:<포트>`,
  `http://localhost:<포트>`, `http://[::1]:<포트>`, 이 Mac 의 Tailscale 주소 `http://<Tailscale 주소>:<포트>` 중
  하나와 스킴·호스트·포트까지 같아야 한다. MagicDNS 이름으로 접속하려면 `IRIS_ALLOWED_ORIGIN_HOSTS` 에
  그 호스트 이름을 적는다(이 목록은 호스트 이름만 대조한다). `Origin` 이 없는 요청(CLI·MCP)은 허용하므로,
  이 Mac 에서 실행되는 다른 프로그램은 로컬 클라이언트로 접속할 수 있다.
- 공증이 없고, 서명은 빌드하는 기계에 따라 달라진다. 저장소는 서명 신원을 지정하지 않는다.
  그래서 `pnpm dist` 는 빌더의 키체인에 있는 인증서를 그대로 쓴다. Apple Development 인증서가 있는
  기계에서는 그것으로 서명되고(그 서명에는 빌더의 신원이 들어간다), 없으면 서명되지 않는다.
  어느 쪽이든 공증은 하지 않으므로 Gatekeeper는 거절한다(`spctl -a` 확인). 직접 빌드해 쓰는
  것을 전제로 한다. 빌드한 앱을 다른 사람에게 그대로 전달하는 것은 이 전제 밖이다.

## 취약점 제보

보안 문제를 발견하면 공개 이슈로 올리기 전에 비공개로 알려 주기 바란다. 제보 경로는 GitHub 저장소의
Security 탭 → Report a vulnerability 이고, 메일은 deancontactadd@gmail.com 이다. 재현 절차와 영향
범위를 함께 적어 주면 확인이 빠르다.

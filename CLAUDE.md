# Iris

## 시작하기 전에

설치된 `/Applications/Iris.app` 과 그 서버는 사용자가 현재 사용 중인 도구다. 이 저장소를 수정하는
작업은 대부분 그 도구에 영향을 준다. 서버 프로세스는 앱이 관리한다. 앱이 시작할 때 자식
프로세스로 실행하고, 앱이 종료될 때 함께 종료한다(`native/electron/server-host.cjs`). 저장소
소스를 직접 읽는 launchd 잡(KeepAlive)이 서버를 관리하는 기기에서는 `server/` 나 `web/` 을 저장하는
즉시 실제 앱이 바뀌므로, 그 잡을 먼저 내린다(아래 「서버 재시작 절차」). 앱이 서버를 관리하는
기기에서는 저장한 내용이 다음 앱 실행부터 반영된다. 실험은 개발 환경에서 한다.

## 실제 앱의 중단 시간 최소화

- 빌드를 끝낸 뒤에 종료한다. 빌드는 `dist/` 에만 쓰므로 앱을 켜 둔 채 빌드하고, 산출물을 확인한
  뒤에만 종료한다. 종료 후 빌드하면 빌드 시간(1~2분) 동안 앱을 쓸 수 없다.
- 서버만 수정했으면 앱을 종료하지 않는다. 서버만 재시작하면 앱이 WebSocket 으로 다시 연결한다.
- 서버를 내리는 동안 앱 창을 닫지 않는다. 재연결 중에 창이 사라지면 메인 프로세스가 종료되어
  오류 창이 반복해서 뜬 사례가 있다(강제 종료가 필요했다).
- 되돌릴 수 없는 교체는 기존 것을 남겨 두고, 새 것이 완전히 준비된 뒤에 지운다. 프로세스가 뜨기를
  기다릴 때는 고정 `sleep` 대신 실제 상태를 확인한다.
- 앱 반영은 `pnpm install:app` 한 명령으로 한다. 종료·빌드·복사를 수동으로 반복하지 않는다.

## 개발 환경과 설치 앱의 분리

이 분리는 데이터 보존 조건이다. 두 환경이 같은 상태 폴더를 쓰다가 데이터가 두 번 삭제된 사례가 있다.

- 개발 환경은 전용 포트와 전용 상태 폴더만 쓴다: `pnpm dev`(4291, `~/.iris-dev`), `pnpm dev:app`.
  CLI·MCP 를 개발 환경에 연결할 때도 `IRIS_PORT`·`IRIS_STATE_DIR` 를 함께 지정한다.
- 상태를 읽고 쓰는 파일을 새로 만들 때는 경로를 직접 조합하지 말고 `server/state-home.cjs` 의
  `stateHome()` 을 호출한다. 한 파일이라도 경로를 직접 지정하면 분리 전체가 깨진다. 과거
  `browser-state.js`·`space-key.js` 가 그랬고, 그 결과 개발 서버가 설치 앱의 북마크·탭·스페이스
  키를 그대로 사용했다. 검사가 추적 파일 전체를 확인하므로 새 파일도 자동으로 검사 대상이 된다.
- 상태 폴더의 소유자는 하나다. 포트가 겹쳐도 바인딩이 항상 충돌하지는 않아서(`0.0.0.0` 과
  `127.0.0.1`) 서버 둘이 같은 파일을 서로 덮어쓴 사례가 있다. 상태 폴더의 `server.lock` 을 획득한
  서버만 실행된다. 이 잠금 파일을 삭제하지 않는다.

## 서버 재시작 절차

서버 프로세스는 앱이 관리하므로 서버만 따로 종료·재시작하지 않는다. 앱을 재시작하면 서버도 함께
재시작된다. 소스를 반영하려면 `pnpm install:app` 을 실행한다.

`kill` 이나 `pkill -f "node server/index.js"` 로 서버를 종료하지 않는다. 후자는 절대 경로로 실행된
프로세스를 찾지 못하고, 둘 다 종료되지 않은 서버 프로세스를 남길 수 있다. 그 프로세스가 이전
상태로 상태 파일을 덮어써서, 복구한 계정·검색 기록·탭이 다시 사라진 사례가 있다.

이전 launchd 잡이 남아 있는 기기에서는 그 잡을 먼저 내린다: `launchctl bootout gui/$UID/<잡 이름>`.
잡이 살아 있으면 KeepAlive 가 종료된 서버를 즉시 다시 실행한다.

공유 상태를 수정하기 전에는 그 상태를 쓰는 프로세스가 하나뿐인지 `ps` 로 확인한다.

## 기능과 앱 셸의 분리

항상 로드되는 앱 셸과, 사용자가 켜고 끌 수 있는 기능으로 나뉜다. 기능은 `web/js/core/capabilities.js`
표에 한 항목으로 등록되고 동적 import 로만 로드된다. 기능을 끄면 그 모듈은 로드되지 않는다. 화면이
있는 기능은 `web/js/core/rail-items.js` 에도 등록한다.

기능을 추가하거나 수정할 때 앱 셸을 수정하지 않는다. 앱 셸이 기능의 동작을 호출해야 하면
`core/hooks.js` 의 이름으로 호출한다. 두 기능이 같은 파일을 공유하지 않는다. 그래야 두 기능을 동시에
작업해도 같은 파일에서 충돌하지 않는다.

이 규칙은 검사로 강제한다. 계약과 각 검사가 막는 항목은 `docs/capabilities.md` 를 참고한다.

## 규칙은 검사로 강제한다

이 저장소는 규칙을 `bin/smoke.mjs` 검사로 강제한다. 위 규칙들도 검사가 있다. 규칙을 바꾸면 검사도
함께 바꾸고, 새 검사는 위반 상태를 만들어 실제로 실패하는지 확인한 뒤에 통과를 신뢰한다.

상세: `docs/two-flows.md`

## 다른 세션에 사용자 입력 전달

세션 간 전달 경로는 둘이고 할 수 있는 일이 다르다. `SendMessage` 는 LLM 이 LLM 에게 전달하며 수신
측 하네스에는 아무 일도 일어나지 않는다. `pane send` 는 상대 입력창에 사용자가 입력한 것처럼 넣고
Enter 를 누른다. 그래서 `/compact`·`/mcp reconnect`·`/clear` 처럼 하네스가 처리하는 명령은 이
경로로만 실행할 수 있다.

연결 방법과 주의 사항: `docs/pane-send.md`

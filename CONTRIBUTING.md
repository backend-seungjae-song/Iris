# 기여

## 개요

이 저장소는 규칙을 문장으로만 두지 않는다. 규칙에는 대체로 그것을 지키는 검사가 있고,
`bin/smoke.mjs` 가 그 대부분을 실행한다. 규칙을 바꾸면 검사도 같이 바꾼다.

새 검사를 만들면 위반 상태를 만들어 실제로 실패하는지 확인한 뒤에 통과를 신뢰한다. 한 번도
실패해 본 적 없는 검사는 무엇을 지키는지 증명된 적이 없다.

## 사전 준비

```bash
pnpm install
pnpm test        # smoke + 오프라인 테스트
```

`pnpm test` 가 통과하는 상태에서 시작하고, 통과하는 상태로 남긴다.

배포용으로 낼 때는 `pnpm release-audit` 이 전수 검사를 한 번에 실행한다. 저장소 상태, 기능
그래프, 개인 정보, 계약 검사, 라이선스 고지를 확인한다. `--full` 을 붙이면 공개본을 새 폴더에
내어 그 안에서 설치·검사가 성립하는지까지 확인한다.

개인 정보 검사(`bin/check-public-hygiene.mjs`)는 두 단계다. 형식 검사(메일 주소, 홈 경로, 허용
목록 밖 호스트, 작업 기록 경로)는 어느 트리에서나 실행된다. 자기 이름, 회사, 기기 이름처럼
이 기기에서만 아는 단어는 git 이 추적하지 않는 `scripts/private-traces.local.mjs` 에 정규식
하나로 둔다:

```js
export const TRACES = /내이름|회사이름|기기이름/i;
```

그 파일이 없으면 검사는 형식 검사만 하고 그 사실을 `note` 로 출력한다. `pnpm export:public` 은
그 상태로는 내보내지 않으며, 형식 검사만으로 내보내려면 `--shape-only` 를 명시해야 한다.

## 새 코드는 진입점에서 참조되어야 한다

파일을 만들고 호출하는 줄을 빠뜨리면 아무 증상이 없다. 파일은 있고 기능은 없는 상태로 검사가
전부 통과한다. 그래서 진입점에서 참조되지 않는 파일과 아무도 호출하지 않는 export 는 검사가
막는다.

```bash
node bin/graph.mjs             # 파일·간선·진입점·고아 목록
node bin/graph.mjs --check     # 게이트 (pnpm release-audit 이 호출한다)
node bin/graph.mjs --why <파일>  # 진입점에서 이 파일까지의 참조 경로
```

진입점은 대체로 손으로 적지 않는다. `package.json` 의 scripts, html 의 script·import, 셸의
호출, 러너의 폴더 탐색에서 읽어 온다. 사람이 직접 실행하는 도구(CLI·조사기)는 호출하는 코드가
없으므로 `docs/entrypoints.md` 에 한 줄 적는다. 셔뱅으로 대신하지 않는다. 셔뱅은 누구나 붙일 수
있어 게이트가 되지 못하고, 그러면 쓰지 않는 도구도 계속 사용 중인 것으로 보인다.

새 코드 폴더를 만들면 `bin/graph.mjs` 의 `CODE_ROOTS` 에 넣는다. 넣지 않으면 그 폴더 전체가
집계에서 제외되어 고아가 있어도 보이지 않는다. 그 목록이 실제 코드 폴더를 모두 포함하는지도
검사가 확인한다.

## 문서가 가리키는 경로와 검사 이름은 실재해야 한다

문서가 오래되는 가장 흔한 형태는 지워진 파일을 계속 가리키는 것이다. 추적되는 `.md` 가
가리키는 코드 경로는 전부 실재해야 하고, `node bin/graph.mjs --doc-drift` 가 그것을 검사한다.
예시 경로는 실물이 없는 것이 정상이므로 「예시」라고 밝힌 블록만 면제된다. 밝히지 않으면 검사가
잡는다.

두 번째 형태는 「이 규칙은 저 검사가 막는다」고 적어 두고 그 검사가 사라지는 것이다. 이쪽이 더
나쁘다. 읽는 사람이 막혀 있다고 믿고 더 확인하지 않는다. 문서가 괄호로 검사 이름을 적는 관례를
쓰려면 그 문서 안에서 그렇게 쓴다고 먼저 밝힌다(`docs/capabilities.md` 가 그 예다). 밝힌 문서의
검사 이름은 전부 실재해야 하고, 「검사 이름을 대는 문서가 실재하는 이름을 댄다」가 그것을
검사한다. 밝히지 않은 문서는 이 규칙 밖이다.

## 코드 규약

- 주석은 무엇을 하는지가 아니라 왜 그렇게 되어 있는지를 적는다. 특히 확인 결과로 알게 된 것
  (무엇이 어떻게 깨졌는지, 어떤 값이 실제로 관측됐는지)은 그 자리에 남긴다. 이 저장소의 주석
  대부분이 그런 기록이고, 다음 사람이 같은 문제를 다시 겪지 않게 하는 수단이다
- 기존 코드의 관례를 따른다. 주석 밀도, 이름 짓기, 관용구를 주변과 맞춘다
- 문서와 코드가 일치하지 않으면 둘 중 하나가 거짓이다. 같은 커밋에서 맞춘다

## 검사가 없는 영역

화면에서 사람이 봐야만 판정되는 항목이 있다. `docs/iris-sequential-audit.md` 가 그 목록이고,
항목마다 자동 근거와 사람 확인을 함께 요구한다. 자동 검사를 통과했다는 것만으로 그 항목을
닫지 않는다.

## 개발 환경

개발은 전용 포트와 전용 상태 폴더로 실행한다(`pnpm dev`, 4291, `~/.iris-dev`).
설치해 쓰는 앱과 같은 상태 폴더를 쓰면 데이터가 섞인다. 상세는 `docs/two-flows.md`.

## 커밋

- 무엇을 왜 바꿨는지 적는다. 증상만이 아니라 원인과 근거(확인된 값, 파일 위치)를 남긴다
- 한 커밋은 한 가지를 한다

## 공개 저장소로 옮기기

수정은 작업 저장소에서 하고, 공개 저장소에는 결과만 한 커밋으로 보낸다. `pnpm sync:public --to
<공개 작업 폴더>` 가 정리본을 내어 그 폴더의 추적 파일을 통째로 바꾸고 커밋한다. 커밋 시각은
`--date`로 지정한다. 생략하면 한국 시간 09:00 이상 19:00 미만에는 해당일 00:00로 기록하고
그 밖에는 실제 시각을 쓴다. 작성자·커미터 시각에 같은 값을 적용한다. 작성자는 그 폴더의 git 설정에서 읽는다. `--push`를 붙여야
원격에 올라간다.

### 공개 저장소에서 가져오기

공개 저장소에 병합된 변경(PR 등)은 한 명령으로 작업 저장소에 적용한다.

```bash
pnpm pull:public -- --from ~/Projects/Personal/iris-public   # --dry-run 이면 무엇이 들어올지만 본다
```

같은 폴더에서 `HEAD..origin/master` 의 차이를 작업 트리에 스테이징하고(같은 자리를 이미
고쳤으면 충돌을 남기고 멈춘다), 그 폴더를 `origin/master` 로 맞춘다. 검사를 실행한 뒤 직접
커밋하면 다음 `pnpm sync:public` 이 그 위에 쌓인다.

## 풀 리퀘스트

- 저장소를 포크해 브랜치를 하나 만들고, 한 브랜치에는 한 주제만 담는다
- 보내기 전에 `pnpm test` 가 전부 통과해야 한다. 검사를 바꿨으면 위반 상태에서 실제로 실패하는지 확인한 결과를 설명에 적는다
- 설명에는 무엇이 왜 바뀌는지, 사람이 앱에서 확인해야 하는 것이 있으면 그 절차를 적는다
